apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 10-mongodb-secret.yaml
  - 20-mongodb-vm.yaml
  - 30-mongodb-service.yaml
  - 35-nationalparks-build.yaml      # S2I BuildConfig (image build)
  - 40-nationalparks.yaml
  - 45-nationalparks-pipeline.yaml   # Tekton CI pipeline (orchestrates clone+test+trigger)
  - 50-parksmap.yaml
  - 55-weather-build.yaml
  - 60-weather-knative.yaml
  # 60-weather-deploy.yaml — alternate (regular Deployment); not used now
